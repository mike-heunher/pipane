diff --git a/node_modules/@mariozechner/pi-web-ui/src/components/AgentInterface.ts b/node_modules/@mariozechner/pi-web-ui/src/components/AgentInterface.ts
index fd4872a..9e43d65 100644
--- a/node_modules/@mariozechner/pi-web-ui/src/components/AgentInterface.ts
+++ b/node_modules/@mariozechner/pi-web-ui/src/components/AgentInterface.ts
@@ -203,8 +203,15 @@ export class AgentInterface extends LitElement {
 		this._lastClientHeight = clientHeight;
 	};
 
+	/**
+	 * When true, sendMessage() bypasses the isStreaming guard,
+	 * allowing messages to be sent while the agent is running (steering).
+	 */
+	@property({ type: Boolean }) allowSendDuringStreaming = false;
+
 	public async sendMessage(input: string, attachments?: Attachment[]) {
-		if ((!input.trim() && attachments?.length === 0) || this.session?.state.isStreaming) return;
+		if (!input.trim() && attachments?.length === 0) return;
+		if (this.session?.state.isStreaming && !this.allowSendDuringStreaming) return;
 		const session = this.session;
 		if (!session) throw new Error("No session set on AgentInterface");
 		if (!session.state.model) throw new Error("No model set on AgentInterface");
@@ -354,6 +361,7 @@ export class AgentInterface extends LitElement {
 					<div class="max-w-3xl mx-auto px-2">
 						<message-editor
 							.isStreaming=${state.isStreaming}
+							.allowSendDuringStreaming=${this.allowSendDuringStreaming}
 							.currentModel=${state.model}
 							.thinkingLevel=${state.thinkingLevel}
 							.showAttachmentButton=${this.enableAttachments}
diff --git a/node_modules/@mariozechner/pi-web-ui/src/components/MessageEditor.ts b/node_modules/@mariozechner/pi-web-ui/src/components/MessageEditor.ts
index 78e4423..48ec6a3 100644
--- a/node_modules/@mariozechner/pi-web-ui/src/components/MessageEditor.ts
+++ b/node_modules/@mariozechner/pi-web-ui/src/components/MessageEditor.ts
@@ -39,6 +39,21 @@ export class MessageEditor extends LitElement {
 	@property() onModelSelect?: () => void;
 	@property() onThinkingChange?: (level: "off" | "minimal" | "low" | "medium" | "high") => void;
 	@property() onFilesChange?: (files: Attachment[]) => void;
+	/**
+	 * Custom keyboard handler. Called before the default handler.
+	 * Return true to prevent the default keydown behavior.
+	 */
+	@property() onKeyDown?: (e: KeyboardEvent) => boolean;
+	/**
+	 * When true, the send button is shown alongside the stop button during streaming,
+	 * and Enter sends even while streaming (for steering messages).
+	 */
+	@property({ type: Boolean }) allowSendDuringStreaming = false;
+	/**
+	 * Optional extra buttons to render in the right toolbar area,
+	 * between the model selector and the send/stop button.
+	 */
+	@property() extraToolbarButtons?: () => import("lit").TemplateResult;
 	@property() attachments: Attachment[] = [];
 	@property() maxFiles = 10;
 	@property() maxFileSize = 20 * 1024 * 1024; // 20MB
@@ -60,9 +75,13 @@ export class MessageEditor extends LitElement {
 	};
 
 	private handleKeyDown = (e: KeyboardEvent) => {
+		// Custom handler gets first crack — return true to suppress default behavior
+		if (this.onKeyDown?.(e)) return;
+
 		if (e.key === "Enter" && !e.shiftKey) {
 			e.preventDefault();
-			if (!this.isStreaming && !this.processingFiles && (this.value.trim() || this.attachments.length > 0)) {
+			const canSend = this.allowSendDuringStreaming || !this.isStreaming;
+			if (canSend && !this.processingFiles && (this.value.trim() || this.attachments.length > 0)) {
 				this.handleSend();
 			}
 		} else if (e.key === "Escape" && this.isStreaming) {
@@ -370,9 +389,20 @@ export class MessageEditor extends LitElement {
 								`
 								: ""
 						}
+						${this.extraToolbarButtons?.() ?? ""}
 						${
 							this.isStreaming
 								? html`
+									${this.allowSendDuringStreaming ? html`
+										${Button({
+											variant: "ghost",
+											size: "icon",
+											onClick: this.handleSend,
+											disabled: (!this.value.trim() && this.attachments.length === 0) || this.processingFiles,
+											children: html`<span style="transform: translateY(3px) rotate(-45deg); display: inline-flex;">${icon(Send, "sm")}</span>`,
+											className: "h-8 w-8",
+										})}
+									` : ""}
 									${Button({
 										variant: "ghost",
 										size: "icon",
@@ -387,7 +417,7 @@ export class MessageEditor extends LitElement {
 										size: "icon",
 										onClick: this.handleSend,
 										disabled: (!this.value.trim() && this.attachments.length === 0) || this.processingFiles,
-										children: html`<div style="transform: rotate(-45deg)">${icon(Send, "sm")}</div>`,
+										children: html`<span style="transform: translateY(3px) rotate(-45deg); display: inline-flex;">${icon(Send, "sm")}</span>`,
 										className: "h-8 w-8",
 									})}
 								`
diff --git a/node_modules/@mariozechner/pi-web-ui/src/components/Messages.ts b/node_modules/@mariozechner/pi-web-ui/src/components/Messages.ts
index 485c513..a92f2ef 100644
--- a/node_modules/@mariozechner/pi-web-ui/src/components/Messages.ts
+++ b/node_modules/@mariozechner/pi-web-ui/src/components/Messages.ts
@@ -56,7 +56,7 @@ export class UserMessage extends LitElement {
 		const content =
 			typeof this.message.content === "string"
 				? this.message.content
-				: this.message.content.find((c) => c.type === "text")?.text || "";
+				: (this.message.content.find((c) => c.type === "text") as { type: "text"; text: string } | undefined)?.text || "";
 
 		return html`
 			<div class="flex justify-start mx-4">
diff --git a/node_modules/@mariozechner/pi-web-ui/src/utils/model-discovery.ts b/node_modules/@mariozechner/pi-web-ui/src/utils/model-discovery.ts
index 299aa9a..b35ef36 100644
--- a/node_modules/@mariozechner/pi-web-ui/src/utils/model-discovery.ts
+++ b/node_modules/@mariozechner/pi-web-ui/src/utils/model-discovery.ts
@@ -232,8 +232,8 @@ export async function discoverLMStudioModels(baseUrl: string, _apiKey?: string):
 					api: "openai-completions" as any,
 					provider: "", // Will be set by caller
 					baseUrl: `${baseUrl}/v1`,
-					reasoning: model.trainedForToolUse || false,
-					input: model.vision ? ["text", "image"] : ["text"],
+					reasoning: (model as any).trainedForToolUse || false,
+					input: (model as any).vision ? ["text", "image"] : ["text"],
 					cost: {
 						input: 0,
 						output: 0,
